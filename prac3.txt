cd ~/Downloads
unzip spring-swarm.zip
cd spring-swarm

ls
pom.xml  src  mvnw
find src/main/java -type d
nano src/main/java/com/demo/spring_swarm/SpringSwarmApplication.java

SpringSwarmApplication.java
package com.demo.spring_swarm;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@SpringBootApplication
@RestController
public class SpringSwarmApplication {

    @GetMapping("/")
    public String home() {
        return "Hello Spring Boot + Maven + Docker Swarm";
    }

    public static void main(String[] args) {
        SpringApplication.run(SpringSwarmApplication.class, args);
    }
}

nano src/main/resources/application.properties
add this line in applicatin.properties server.port=9090


mvn spring-boot:run


